@model IEnumerable<Infrastructure.Models.Zapato>

@{
    ViewBag.Title = "IndexAdmin";
}

<h2> Calzado</h2>

<p>
    @Html.ActionLink("Añadir Nuevo" , "Create")
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.idZapato)
        </th>
       

        <th>
            @Html.DisplayNameFor(model => model.color)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cantidadTot)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>

    <td>
        @Html.DisplayFor(modelItem => item.idZapato)
    </td>

    <td>
        @Html.DisplayFor(modelItem => item.color)
    </td>
    <th>
        @Html.DisplayFor(modelItem => item.descripcion)
    </th>
    <th>
        @Html.DisplayFor(modelItem => item.cantidadTot)
    </th>
    <td>
        @Html.ActionLink("Editar", "Edit", new { id = item.idZapato }) |
        @Html.ActionLink("Detalles", "Details", new { id = item.idZapato }) |
        @Html.ActionLink("Eliminar", "Delete", new { id = item.idZapato })
    </td>
</tr>
}

</table>
